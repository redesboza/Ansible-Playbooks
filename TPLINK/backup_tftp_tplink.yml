---
- name: Backup startup-config a TFTP (TP-Link)
  hosts: all
  gather_facts: no

  vars:
    tftp_server: "192.168.57.11"

  tasks:
    - name: Copiar startup-config al servidor TFTP (TP-Link)
      ansible.builtin.command:
        argv:
          - python3
          - -u
          - /runner/project/TPLINK/backup_tftp_tplink.py
          - "{{ ansible_host }}"
          - "{{ ansible_user }}"
          - "{{ ansible_ssh_pass }}"
          - "{{ ansible_port | default('11110') }}"
          - "{{ tftp_server }}"
          - "{{ inventory_hostname }}__{{ ansible_host }}"
      delegate_to: localhost
      register: out

    - name: Mostrar salida del script
      ansible.builtin.debug:
        var: out.stdout

    - name: Mostrar log de sesión (últimas 200 líneas)
      ansible.builtin.shell: "ls -1 /tmp/tplink_backup_*.log 2>/dev/null | tail -n 1 | xargs -r tail -n 200"
      delegate_to: localhost
      register: slog
      ignore_errors: yes

    - name: Debug session log
      ansible.builtin.debug:
        var: slog.stdout
